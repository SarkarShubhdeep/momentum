export interface Category {
    cat_id: string;
    cat_name: string;
    cat_color?: string;
    task_count?: number;
    created_at?: string;
    updated_at?: string;
    // Add other fields if needed
}
